#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <directory.h>
#include<regularfile.h>

namespace Ui {
class MainWindow;
}

class MainWindow : public QMainWindow
{
    Q_OBJECT
public:
    directory * dir;
    QList<regularfile> fileList;
    
public:
    explicit MainWindow(QWidget *parent = nullptr);
    ~MainWindow();
    void InitFile();

private:
    Ui::MainWindow *ui;
};

#endif // MAINWINDOW_H
